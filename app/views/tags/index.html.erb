<h1>All Available Site Tags</h1>

<p>
<% if @tags.size == 0 %>
  <h2>No tags have been created</h2>
<% end %>
<ul id="tags">
  <% @tags.each do |tag| %>
    <li>
      <%= link_to tag.name, tag_path(tag.id), class: 'article_title' %>
      <%= link_to "Delete", tag_path(tag), method: :delete, \
          data: {confirm: "Really delete the tag?"} %>
    </li>
  <% end%>
</ul>
<p>
<%= link_to "<< Back to Articles", articles_path %>